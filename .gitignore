node_modules
dist
dist-ssr

# Deploy-Paket für den manuellen Netlify-Upload. Entsteht aus `dist/` und
# wird bei jedem Build neu erzeugt — im Verlauf wäre es ein Binary, das
# sofort veraltet und die Historie dauerhaft aufbläht.
kydon-netlify.zip
.env
.env.local
.env.*.local
*.local
.DS_Store
.vite
coverage
*.tsbuildinfo

# Playwright-Artefakte: Ergebnis eines Laufs, kein Quellstand.
test-results/
playwright-report/

# Mockup-Aufnahmen: Bilder, keine Quelltexte.
mockups/out/
mockups/web/
baseline-mockups.zip
baseline-paletten.zip
baseline-20-paletten.zip
baseline-farbsysteme.zip
baseline-sport.zip
mockups/bilder/
baseline-login.zip
baseline-grafik-neu.zip
